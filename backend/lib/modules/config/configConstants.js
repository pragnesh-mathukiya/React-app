var moduleName = `Site Configuration`;

let messages = {
  createdSuccess: `Configuration successfully`,
  createIssue: `There is issue with configuration`,
  dataNotFound: "No record found",
};

module.exports = {
  MESSAGE: messages,
};
